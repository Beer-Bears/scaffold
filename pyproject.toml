[tool.poetry]
name = "scaffold"
version = "0.1.0"
description = ""
authors = ["Beer Bears"]
readme = "README.md"
package-mode = false

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
